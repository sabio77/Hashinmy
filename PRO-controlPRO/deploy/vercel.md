# Vercel

La semilla incluye `vercel.json` con headers de seguridad y caché.

Si no tienes build real, puedes publicar la raíz como proyecto estático. Si quieres regenerar huellas antes de publicar, agrega como build command:

```bash
python tools/generate-release.py
```
